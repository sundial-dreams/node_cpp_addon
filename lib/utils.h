#ifndef NODE_CPP_ADDON_UTILS_H
#define NODE_CPP_ADDON_UTILS_H

#include <iostream>

int findSubStr(const char* str, const char* subStr); // 查找子串位置
int subStrCount(const char* str, const char* subStr); // 字串在源字符串中出现次数

#endif //NODE_CPP_ADDON_UTILS_H
